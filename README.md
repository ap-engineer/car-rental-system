# car-rental-system
A car rental system that needs to be adaptable to different customers' needs


RentalService/
├── src/
│   ├── Api/                  → ASP.NET Core Web API project
│   ├── Application/          → Business use cases, DTOs, services
│   ├── Domain/               → Core entities, logic
│   └── Infrastructure/       → (optional, if we add persistence later)
└── tests/